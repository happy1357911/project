# 與原始程式碼差異比對

<!-- 2026-06-25-p0-p4-gpt-analysis-update -->

## 2026-06-25 P0-P4 GPT Analysis Response Update

This update was implemented after checking the current codebase against the latest GPT all.zip analysis and professor feedback. The goal is to move the project from a model-score-only framing toward a missing-aware, clinical-rule-guided, locked-test-traceable decision-support pipeline.

Completed updates:
- `sync_all_outputs_v74.py` now declares package type: `analysis_only`, `execution_ready`, or `custom`.
- The sync manifest explicitly records whether raw data, checkpoints, and large row-level prediction files are included.
- `run_all_v74.py` passes package flags to sync and verifies that `sync_manifest_v74.json` matches the run arguments.
- `run_locked_test_v74.py` now produces locked-test error analysis, three-way conflict tables, calibration summary, ECE bins, Brier score, and confidence-threshold curves.
- `run_all_v74.py` now creates `results_v74/calibration_analysis_locked_test/` and runs error analysis with `--mode all`.
- Hybrid outputs now include `hybrid_decision_reason`, `hybrid_warning_reasons`, and decision-reason summary CSVs.
- Artificial missingness now applies conservative scenario-level model fallback for incomplete or corrupted stress scenarios.
- Task2 artificial ABG borderline stress now uses 10 dB instead of 15 dB, so it is truly borderline rather than clear ABG.
- Error analysis now outputs `decision_safety_summary.csv`.
- Dashboard now displays model confidence, hybrid decision reason, and low-confidence abstention.
- `evaluate_v74.py` now outputs `statistical_summary_all_configs.csv` with mean, std, SEM, and clipped 95 percent CI.

Recommended formal refresh command:

```powershell
python run_all_v74.py --skip-model-profile --run-locked-test --locked-allow-overwrite --package-type analysis_only
```

Use this only when a re-runnable large package is required:

```powershell
python run_all_v74.py --skip-model-profile --run-locked-test --locked-allow-overwrite --package-type execution_ready
```

<!-- /2026-06-25-p0-p4-gpt-analysis-update -->

### Main Differences From Previous State
The package manifest is now reproducibility-aware, locked-test post-processing is complete, hybrid decisions are explainable, artificial missingness has conservative fallback, and CI summaries are generated.


<!-- 2026-06-20-edge-profile-deferred -->

## 2026-06-20 決策：暫不執行 edge 端估計

目前先不把 edge/deployment latency profile 作為本輪必跑項目。主流程優先保留模型訓練、locked-test、rule/model/hybrid、missingness robustness、calibration 與 baseline 結果；`model_profile_v74.py` 與 `results_v74/model_profile/` 先列為後續需要 edge / IoMT deployment 敘事時再單獨補跑的項目。

<!-- 2026-06-19-codex-update -->

## 2026-06-19 Update: P0/P1 and full pipeline

Status: P0/P1 code changes are implemented and checked against the actual scripts, not only against the suggestion text.

Implemented changes:
- `clinical_rules_v74.py`: added `RuleDecision` for rule label, coverage, confidence, compatible labels, and warning flags.
- `preprocessing_v74.py`, `train_v74.py`, `dashboard_three_tasks_metaIRL_v74.py`, and `error_analysis_v74.py`: aligned rule/reward/dashboard/error-analysis logic with the shared rule decision.
- `train_v74.py`: added training-only structured missingness augmentation and added `run_06_rr_k1_small_missingaug_r010` plus `run_07_rr_k1_small_missingaug_noirl`.
- `artificial_missingness_v74.py`: added structured Task2/Task3 missingness scenarios and model-only/rule-forced/rule-abstain-as-error/hybrid-rule-first strategy outputs.
- `ml_baselines_v74.py`: added `ml_baseline_summary_5seed.csv` and `ml_baseline_per_class_5seed.csv` aliases.
- `model_profile_v74.py`: added `model_profile_summary.csv` for latency, parameters, checkpoint size, and estimated FP32 parameter memory.
- `hybrid_evaluation_v74.py`: separated no-support and locked-test output filenames so locked-test no longer overwrites `main_hybrid_summary.csv`.
- `run_all_v74.py`: reordered the real pipeline as compile -> split protocol -> train with locked manifest -> baselines -> error/evaluate -> no-support hybrid -> locked-test hybrid -> calibration -> artificial missingness -> model profile -> sync -> verify.

Main command:
```powershell
& "C:\Users\ASUS\anaconda3\envs\project2\python.exe" run_all_v74.py --python "C:\Users\ASUS\anaconda3\envs\project2\python.exe"
```

Verification already completed:
- project2 venv full `py_compile` passed.
- `run_all_v74.py --dry-run` showed the corrected order and complete command chain.
- `sync_all_outputs_v74.py` required-file list check returned `missing_files=[]`.

<!-- /2026-06-19-codex-update -->


更新日期：2026-06-12  
適用版本：目前 v7.4 專案、正式輸出與 `all/` 同步狀態

本文件不是逐行 diff，而是整理目前版本相對原始版本的主要功能差異、資料處理差異、輸出差異與仍需注意的部分。

## 1. 整體差異總覽

| 類別 | 原始/早期狀態 | 目前狀態 |
|---|---|---|
| 資料單位 | 多處仍以 row-level 或左右耳同列處理 | Task1/Task2/Task3 都轉為 ear-level sample |
| Split | 較容易只看一般 train/valid | 使用 `case_id` group split，避免左右耳 leakage；另支援 locked split manifest |
| Task2 label | 混合 hearing type 與 high/low tone configuration | 僅保留 `WNL/SNHL/CHL/MHL` |
| Task2 feature | 曾使用 PTA/mean/slope 或整列左右耳 feature | 目前單耳 AC/BC/ABG 四頻率與 flags，共 32 個 |
| Task2 rule | 曾分散在 train/baseline/dashboard | 集中於 `clinical_rules_v74.py` |
| Shared preprocessing | train、baseline、dashboard 曾各自處理 | 新增 `preprocessing_v74.py` 共用主要轉換、evidence status、warning summary |
| Task3 rule | 曾在不同位置判斷 NP/missing | 集中於 shared preprocessing / clinical rule 流程 |
| Evaluation | 主要 configured validation | 新增 deployment no-support evaluation 與 locked-test no-support output |
| Dashboard | 曾有自己的 rule / support 設定 | 目前固定 `support=None`，並顯示 warning / conflict / evidence |
| Baseline | 早期只有 forced rule baseline | 新增 missing-aware baseline 與 classical ML baseline |
| Error analysis | 早期缺少逐筆 subgroup/error output | 新增 subgroup、conflict、Task2 clinical focus |
| Split protocol | 早期缺少正式 manifest | 新增 `split_protocol_v74.py` |
| Artificial missingness | 無 | 新增缺失壓力測試 |
| Model profile | 無 | 新增參數量、checkpoint size、CPU latency |
| all 同步 | 需手動整理 | 新增 `sync_all_outputs_v74.py` |

## 2. 新增與修改的主要檔案

| 檔案 | 主要差異 |
|---|---|
| `train_v74.py` | 新增 ear-level preprocessing wrapper、no-support final evaluation、prediction rows、reward confidence、summary_no_support aggregation、single-task aliases、locked-test no-support evaluation、ablation preset。 |
| `preprocessing_v74.py` | 新增 shared preprocessing、model row builder、evidence status、clinical warning summary。 |
| `clinical_rules_v74.py` | 新增 shared clinical rules，集中 Task2/Task3 判斷與數值解析。 |
| `baseline_rules_v74.py` | 改為 missing-aware baseline，輸出 forced、covered-only、abstain-as-error。 |
| `ml_baselines_v74.py` | 新增 classical ML baseline，並使用 shared preprocessing wrapper。 |
| `dashboard_three_tasks_metaIRL_v74.py` | 匯入 shared preprocessing / warning，推論固定 `support=None`，輸出 rule/conflict/evidence 欄位。 |
| `evaluate_v74.py` | 新增 no-support 與 locked-test no-support paper table export。 |
| `error_analysis_v74.py` | 新增 subgroup/error analysis、rule-model conflict、Task2 clinical subgroup、Task2 confusion focus。 |
| `split_protocol_v74.py` | 新增 grouped split / locked test manifest 腳本。 |
| `artificial_missingness_v74.py` | 新增人工缺失情境測試。 |
| `hybrid_evaluation_v74.py` | 新增 model-only / rule / hybrid 比較。 |
| `model_profile_v74.py` | 新增參數量、checkpoint size、latency profile。 |
| `sync_all_outputs_v74.py` | 新增同步工具，將主要檔案與正式輸出複製到 `all/`。 |
| `mtl_meta_irl_transformer.py` | 修正 prototype/context 與不同 `d_model` 設定相容問題。 |

## 3. Task2 clinical rule 差異

目前 Task2 不再使用 PTA、`ac_mean`、`bc_mean`、`abg_mean` 做 hearing type rule。

目前使用：

- 500Hz
- 1000Hz
- 2000Hz
- 4000Hz

規則：

1. 任一有效頻率 `ABG > 10` 視為有 air-bone gap。
2. 有 ABG：
   - 任一有效 BC `> 25`：MHL
   - 否則：CHL
3. 無 ABG：
   - 任一有效 AC `> 25`：SNHL
   - 否則：WNL
4. BC missing 不視為 0，也不產生 fake ABG。
5. AC/BC NR 或 ABG censored 會標記為 uncertain evidence，降低 reward confidence。

目前 Task2 rule 對 CSV：

| 指標 | 結果 |
|---|---:|
| prepared rows | 174 |
| label/rule mismatch | 0 |
| uncertain evidence rows | 75 |

## 4. Feature 差異

目前 union feature 數為 49。

| Task | 現在 features | 差異 |
|---|---:|---|
| Task1 | 5 | 單耳 AC 四頻 + PTA |
| Task2 | 32 | 單耳 AC/BC/ABG 四頻率 + NR/missing/censored flags |
| Task3 | 16 | Tympanogram raw values + real_zero/missing_zero/np_zero flags |

Task2 已排除：

- `ac_mean`
- `bc_mean`
- `abg_mean`
- `abg_mean_censored`

原因：Task2 hearing type 主要是圖形/逐頻率判讀，不宜用平均值直接代表。

## 5. Loss 與 reward 差異

目前 loss：

```text
task_loss = exp(-log_var_task) * CE_loss
            + log_var_task
            + reward_weight * reward_loss
```

目前新增的重點：

- `reward_loss` 不再只是固定 target。
- `expert_consistency_target_and_confidence()` 會回傳 target 與 confidence。
- Task2 若 evidence missing/NR/censored，reward confidence 下降。
- Task3 若 rule evidence partial/missing，reward confidence 下降。

目的：讓 reward 作為輔助，不主導 CE loss。

## 6. Evaluation 差異

每個 run 現在會輸出：

| 檔案 | 說明 |
|---|---|
| `eval_summary.json` | configured validation，可能使用 meta support。 |
| `eval_summary_no_support.json` | deployment no-support，固定 `support=None`。 |
| `eval_summary_locked_test_no_support.json` | 若使用 locked split manifest，輸出 locked test no-support。 |
| `per_class_metrics.csv` | configured per-class metrics。 |
| `per_class_metrics_no_support.csv` | no-support per-class metrics。 |
| `per_class_metrics_locked_test_no_support.csv` | locked test no-support per-class metrics。 |
| `prediction_rows.csv` | configured 逐筆預測。 |
| `prediction_rows_no_support.csv` | no-support 逐筆預測。 |
| `prediction_rows_locked_test_no_support.csv` | locked test no-support 逐筆預測。 |

run config 層級現在可彙整：

- `summary.csv`
- `summary_no_support.csv`
- `summary_locked_test_no_support.csv`
- `all_runs_metrics.csv`
- `all_runs_metrics_no_support.csv`
- `all_runs_metrics_locked_test_no_support.csv`

## 7. 新增獨立分析腳本

| 腳本 | 正式輸出位置 | 目前狀態 |
|---|---|---|
| `baseline_rules_v74.py` | `results_v74/rule_baselines_phase3/` | 已正式產生 |
| `ml_baselines_v74.py` | `results_v74/ml_baselines/` | 已正式產生 |
| `error_analysis_v74.py` | `paper_v74/error_analysis/` | 已正式產生 |
| `split_protocol_v74.py` | `results_v74/split_protocol/` | 已正式產生 |
| `artificial_missingness_v74.py` | `results_v74/artificial_missingness/` | 已正式產生 |
| `hybrid_evaluation_v74.py` | `paper_v74/hybrid_evaluation/` | 已正式產生 |
| `model_profile_v74.py` | `results_v74/model_profile/` | 已正式產生 |

## 8. Ablation 差異

目前新增 `ABLATION_RUN_CONFIGS`：

- `ablation_tiny_d32_l1_r010`
- `ablation_tiny_d32_l1_r000`
- `ablation_small_d64_l2_r010`

CLI 支援：

```powershell
python train_v74.py --config_preset recommended
python train_v74.py --config_preset ablation
python train_v74.py --config_preset all
```

single-task 支援：

```powershell
python train_v74.py --experiments single_task --single_task_target Task1
python train_v74.py --experiments single_task --single_task_target Task2
python train_v74.py --experiments single_task --single_task_target Task3
python train_v74.py --experiments single_task --single_task_target all
```

也可使用 aliases：

- `single_task_task1`
- `single_task_task2`
- `single_task_task3`

## 9. 目前仍需注意

| 項目 | 狀態 |
|---|---|
| `artificial_missingness` 的 `scenario=none` | 是全資料 inference，不是 formal validation split。 |
| Task2 CHL | 只有 2 筆，macro-F1 對 split 很敏感。 |
| Task3 C | 樣本少，需搭配 per-class 與 subgroup。 |
| rule baseline | forced rule 很高，但 missing-aware abstain-as-error 更適合作為缺值情境對照。 |
| `all.zip` | 仍是舊壓縮檔，不代表目前最新版 `all/`。 |

## 10. 建議下一步

1. 修正所有報告或簡報中會誤解的指標標示，尤其 `0.984` 應標為 artificial-missingness full-data `scenario=none`。
2. 論文主結果使用 `paper_v74/tables/summary_no_support_all_configs.csv`。
3. 針對 Task2 CHL、Task3 C 報告 support 與 per-class F1。
4. 若要更嚴謹比較 hybrid，補 split-aligned hybrid comparison。
5. 若要投論文，補 locked-test 正式完整訓練結果與 confidence interval。

---

## 2026-06-18 新增差異摘要

本輪相對於先前版本新增/修改：`calibration_analysis_v74.py`、`run_locked_test_v74.py`、`run_all_v74.py`、`sync_all_outputs_v74.py`、`error_analysis_v74.py`、`evaluate_v74.py`、`hybrid_evaluation_v74.py`、`model_profile_v74.py`、`dashboard_three_tasks_metaIRL_v74.py`、`preprocessing_v74.py`。核心差異是從 model-only/MetaIRL 敘事，轉為 rule-first hybrid decision support，並補上 calibration、三方 conflict、low support warning、deployment profile 與 locked-test 安全入口。

注意：`backups/` 與 `outputs/phase11_check/` 保留歷史快照，不代表目前正式狀態；正式狀態以根目錄檔案、`paper_v74/`、`results_v74/`、`gpt_suggestions_action_plan_v74.md` 與 `run_all_v74.py` 為準。

## 2026-06-21 P0-P2 ????
????????????

- ?? `checkpoint_utils_v74.py`??? checkpoint ??????
- ?? `feature_importance_v74.py`?feature group permutation importance ? inference-time ablation?
- ?? `artificial_missingness_v74.py`??? degradation?per-class?evidence compensation?confidence-gated hybrid?
- ?? `hybrid_evaluation_v74.py`??? `hybrid_rule_first_confidence_gate` ? low-confidence abstain metrics?
- ?? `train_v74.py`?Task2/Task3 training missingness augmentation ??????????
- ?? `model_profile_v74.py`?checkpoint resolver ????summary merge key ??????
- ?? `dashboard_three_tasks_metaIRL_v74.py`?checkpoint selection ?? resolver?
- ?? `run_all_v74.py`??? feature importance ? clean all/ sync?
- ?? `sync_all_outputs_v74.py`????? checkpoint??? package inventory manifest?

## 2026-06-21 RUN_CONFIGS / ABLATION_RUN_CONFIGS ??
???? `train_v74.py`?

- `RUN_CONFIGS` ? 7 ??? 15 ??
- `ABLATION_RUN_CONFIGS` ? 3 ??? 5 ??
- ?? `MODEL_SIZE_CONFIGS`?`MISSING_AUG_PROFILES`?`make_run_config()`??? 20 ????????
- `apply_training_missingness_augmentation()` ?? Task1 masking?
- checkpoint metadata ? `missingness_augmentation.tasks` ? Task2/Task3 ?? Task1/Task2/Task3?
- `sync_all_outputs_v74.py` ??????? 15 ? `paper_v74/run_*` ? 5 ? `paper_v74/ablation_*`?
- `checkpoint_utils_v74.py` ? `run_all_v74.py` ?? checkpoint ?? `run_02_base_m10_balanced_r015`?

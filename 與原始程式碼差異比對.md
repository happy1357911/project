<!-- 2026-07-08-hard-warning-taxonomy-update -->
## 2026-07-08 Difference update

Compared with the previous version, this update adds consistency checks around rule-first use rather than changing the model architecture.

- `clinical_rules_v74.py`: adds `has_hard_rule_warning()` as the shared hard-warning helper.
- `hybrid_evaluation_v74.py`: uses the shared helper instead of a local warning-token list.
- `dashboard_three_tasks_metaIRL_v74.py`: uses the shared helper so UI decisions match batch evaluation.
- `artificial_missingness_v74.py`: uses the shared helper, records hard-warning blocked rule decisions, and emits `clinical_missingness_taxonomy.csv`.
- `model_profile_v74.py`: uses the shared helper and reports hybrid rule rate plus hard-warning block rate.
- `error_analysis_v74.py`: emits `task2_rule_label_conflict_audit.csv` for Task2 rule-label conflict inspection.
- `run_all_v74.py`: requires the new taxonomy and conflict-audit outputs during verification.

<!-- /2026-07-08-hard-warning-taxonomy-update -->

<!-- 2026-07-08-directional-abg-rule-gate -->

## 2026-07-08 Current Rule and Hybrid Gate

This block records the current code behavior. If older historical sections mention the old absolute-gap formula, the old 10 dB ABG rule, or direct Task3 B/C hard labels, this block is authoritative.

### Task2 directional ABG

- Task2 rule no longer uses the old absolute-gap formula.
- Meaningful ABG is directional: `AC - BC >= 15 dB`.
- `BC - AC >= 15 dB` is not ABG. It is flagged as `negative_abg_or_measurement_inconsistency` and falls back to the model.
- `10 <= AC - BC < 15 dB` is `abg_borderline`; it does not trigger CHL/MHL and falls back to the model.
- AC 6000/8000 Hz is model input and supplemental warning evidence only. It does not independently decide SNHL in the rule path.

### Task2 rule-first gate

- Forced rule labels may still output WNL/SNHL/CHL/MHL for baseline analysis.
- Official rule-first currently allows only covered `SNHL` and `WNL` cases.
- `MHL` and `CHL` are kept for forced-rule analysis but are blocked from official hybrid rule-first because the current labels and simplified numeric rules still conflict for those classes.
- Missing data, borderline ABG, negative ABG, high-frequency-only abnormality, and unclear rule combinations all set `baseline_covered=False` and fall back to the model.

### Task3 rule-first gate

- `peak NP + compliance NP` remains valid Type B evidence.
- `peak_daPa <= -300` and `-300 < peak_daPa <= -150` are B/C-uncertain and fall back to the model.
- `peak_daPa > -150` can be rule-first Type A only when there is no low-compliance or wide-width warning.
- `compliance < 0.20` or `width >= 200` is A/B-uncertain and falls back to the model.
- Vea missing is an evidence warning only; it does not decide A/B/C by itself.

### Hybrid gate

Official hybrid uses rule only when all conditions hold: valid `rule_decision_label`, `baseline_covered=True`, `complete_for_rule=True`, `rule_confidence >= 0.8`, and no hard warning in `warning_reasons`. Otherwise it uses the model prediction, with low-confidence abstain only when model confidence is below threshold. `rule_forced` is analysis-only and is not the deployed decision policy.

<!-- /2026-07-08-directional-abg-rule-gate -->

<!-- 2026-07-05-device-routing-update -->

## 2026-07-05 與前一版流程差異：GPU/CPU 裝置路由

本次差異只影響執行裝置與 CLI 介面，不改資料前處理、label rule、模型結構、metric 計算或輸出檔案結構。

| 檔案 | 差異 |
|---|---|
| `run_all_v74.py` | 新增 `--device` 與 `--profile-device`，Step 2/9/10 改由 `--device` 控制，Step 11 profile 改由 `--profile-device` 控制。 |
| `train_v74.py` | 新增 `--device auto/cpu/cuda/cuda:<index>`，可強制訓練使用 GPU 或 CPU。 |
| `artificial_missingness_v74.py` | 新增 `--device auto` 支援，並在 manifest 記錄 requested/resolved device。 |
| `feature_importance_v74.py` | 新增 `--device auto` 支援，並在 manifest 記錄 requested/resolved device。 |
| `model_profile_v74.py` | 支援 `auto/cuda`，但預設仍是 CPU，以保留 deployment latency profile 意義。 |
| `run_locked_test_v74.py` | 新增 `--device`，只傳給內部 `train_v74.py`。 |

正式長跑建議使用：

```powershell
python run_all_v74.py --skip-model-profile --device cuda
```

<!-- /2026-07-05-device-routing-update -->
<!-- 2026-07-01-inline-run-configs -->

## 2026-07-01 目前參數設定狀態

本專案已取消外部參數檔。正式訓練參數集中在 `train_v74.py` 內維護：

- `MODEL_SIZE_CONFIGS`：base / small / tiny 的模型大小設定。
- `MISSING_AUG_PROFILES`：Task1、Task2、Task3 的缺失資料 augmentation 權重。
- `RUN_CONFIGS`：15 組正式訓練參數。
- `ABLATION_RUN_CONFIGS`：5 組 ablation 參數。

`train_v74.py` 仍使用 `--config_preset recommended|ablation|all` 選擇要跑正式組、ablation 組或全部 20 組。`run_all_v74.py` 與 `run_locked_test_v74.py` 不再接受外部參數檔路徑。若要改參數或分電腦跑，請直接在同一套程式的 `train_v74.py` 內調整參數組合或自行改要執行的 config，不再使用外部分工檔。同步程式也不再同步舊 config 目錄，正式封包中的舊 config 目錄已清除。

<!-- /2026-07-01-inline-run-configs -->
# 與原始程式碼差異比對

<!-- 2026-06-27-narrative-alignment -->

## 2026-06-27 文字敘事與命名收斂

本段為目前對外說明的優先採信版本。後續給 GPT、教授或論文草稿分析時，請先以本段理解研究定位；較早日期段落若仍出現舊名稱或舊數字，只作歷史紀錄，不作為目前主張。

目前研究主軸不是單純的神經網路架構或模型準確率比較，而是：

```text
Missing-aware clinical-rule-guided hybrid decision-support framework
for ear-level hearing classification under incomplete audiological evidence.
```

中文定位：聽力師規則引導、缺值感知的混合式聽力輔助判讀系統。

目前敘事原則：
- `MetaIRL`、Transformer、prototype/meta-learning 只作為神經網路元件或消融模組，不作為唯一主貢獻。
- 主要臨床問題是 Task2/Task3 在缺失、NR/截尾值、ABG 邊界值、無 peak / 無 tympanogram 等不完整證據下的可靠輔助判讀。
- 混合式決策的價值是規則優先、模型回退、警示、暫不判讀與規則-模型衝突透明化；不可寫成混合式決策已全面高於規則決策。
- 傳統機器學習基準很強；若實驗協定不同，只能說是具競爭力的 grouped validation 基準，不能直接與 locked-test 神經網路結果作公平勝負結論。
- 人工缺失分析目前是強健性壓力測試證據；除非對齊主要設定與 5 seeds，否則不要寫成最終主要強健性結果。
- IoMT / 部署目前應寫成概念性臨床決策輔助流程；邊緣端 latency/profile 不是本輪主軸，device-shift 主張需等有 device-level 驗證後再放入。
- `all/` 是給 GPT/教授分析的封包；可包含目前使用的原始資料，但沒有 checkpoint 與大型逐列預測時，仍不是完整可重跑的 execution-ready 封包。

<!-- /2026-06-27-narrative-alignment -->

<!-- 2026-06-26-目前-difference-alignment -->

## 2026-06-26 目前與早期版本的主要差異

本段是目前差異總結；較早日期段落保留作為歷史紀錄。
目前程式碼對齊重點：
- Task1 資料來源：`task1_all_three_common14_v1.csv`。
- Task2/Task3 資料來源：`task2_3_pure_data(6_24).xlsx`。
- 目前特徵數：Task1 = 5、Task2 = 36、Task3 = 16、三任務 union = 53。
- Task2 模型輸入包含 AC 500/1000/2000/4000/6000/8000 Hz、六頻 AC NR 標記、BC 500/1000/2000/4000 Hz、BC NR/缺失標記、ABG 500/1000/2000/4000 Hz 的 數值/缺失/截尾 標記。
- Task2 規則使用 AC 500/1000/2000/4000 Hz 加上 6000/8000 Hz 至少一個高頻存在作為完整性條件；ABG 以 `AC-BC>=15 dB` 判定 clear ABG，`10<=AC-BC<15 dB` 作邊界警示。
- Task3 current rule: peak <= -150 is B/C-uncertain and falls back to model; clear A requires peak > -150 without low-compliance or wide-width warning.
- 混合式決策 規則優先 改以 `rule_confidence` / `rule_evidence_score` 達門檻且 rule label 存在時採用規則；否則使用模型，若模型信心度低於門檻則可暫不判讀。
- `train_v74.py 內建 RUN_CONFIGS/ABLATION_RUN_CONFIGS` 為完整 20 組設定：15 組 `run_configs` 加 5 組 `ablation_run_configs`。
- `all/` 預設是給 GPT/教授分析的 `analysis_only` 封包；可用 `--include-raw-data` 納入目前使用的原始資料，但未包含 checkpoint 與大型逐列預測 時仍不是完整可重跑 封包。
- 本輪 邊緣端/model profile 仍可用 `--skip-model-profile` 延後，不作為目前主流程必跑項。
目前相對早期版本的核心差異：

| 類別 | 早期狀態 | 目前狀態 |
|---|---|---|
| 資料來源 | Task2/Task3 曾使用舊 CSV | 統一為 `task2_3_pure_data(6_24).xlsx` |
| 特徵 | Task2 曾只使用四頻 AC 或更少衍生欄位 | Task2 目前 36 維，含六頻 AC、BC、ABG 與 缺失/截尾 標記 |
| Rule | Task2/Task3 規則 曾分散於多個模組 | `clinical_rules_v74.py` 集中維護，其他模組共用 |
| NR 語意 | NR/缺失/截尾 曾混用 | `xxNR` 是 measured-but-截尾；缺失 與 截尾 分開 |
| 混合式決策 | 曾只看 rule covered | 現在主要看 `rule_confidence` / `rule_evidence_score`、規則 label 是否存在，以及模型信心度 |
| Config | 曾硬編在 `train_v74.py` | 現在由 `train_v74.py 內建 RUN_CONFIGS/ABLATION_RUN_CONFIGS` 管理；舊外部分工檔已移除 |
| 評估 | 曾偏 configured/no-support | 現在包含 locked-test no-support、校準、決策安全性、三方衝突 |
| 封包 | 曾容易誤認 all.zip 可重跑 | 現在用 `analysis_only` / `execution_ready` manifest 明確區分 |

<!-- /2026-06-26-目前-difference-alignment -->

<!-- 2026-06-25-p0-p4-gpt-analysis-update -->

## 2026-06-25 P0-P4 GPT 分析回應更新

本次更新是在核對目前程式碼、最新版 GPT all.zip 分析與教授回饋後完成。目標是讓專案從只看模型分數的敘事，收斂為缺值感知、臨床規則引導、可追溯 locked-test 的決策輔助流程。

已完成更新：
- `sync_all_outputs_v74.py` 現在會標示封包類型：`analysis_only`、`execution_ready` 或 `custom`。
- sync manifest 會明確記錄是否包含原始資料、checkpoint 與大型逐列預測檔案。
- `run_all_v74.py` 會把封包旗標傳給同步流程，並驗證 `sync_manifest_v74.json` 是否符合執行參數。
- `run_locked_test_v74.py` 現在會產生 locked-test error analysis、三方衝突表、校準摘要、ECE bins、Brier score 與信心度門檻 curves。
- `run_all_v74.py` 現在會建立 `results_v74/calibration_analysis_locked_test/`，並用 `--mode all` 執行 error analysis。
- 混合式決策輸出現在包含 `hybrid_decision_reason`、`hybrid_warning_reasons` 與決策原因摘要 CSV。
- 人工缺失現在會針對 incomplete 或 corrupted stress scenario 採用較保守的 scenario-level 模型回退。
- Task2 artificial ABG 邊界 stress 現在使用 10 dB 而不是 15 dB，使其代表邊界而非明確 ABG。
- Error analysis 現在會輸出 `decision_safety_summary.csv`。
- Dashboard 現在會顯示模型信心度、混合式決策原因與低信心度暫不判讀。
- `evaluate_v74.py` 現在會輸出 `statistical_summary_all_configs.csv`，包含 mean、std、SEM 與 clipped 95% CI。

建議正式刷新指令：

```powershell
python run_all_v74.py --skip-model-profile --run-locked-test --locked-allow-overwrite --package-type analysis_only
```

只有在需要可重跑的大型封包時才使用：

```powershell
python run_all_v74.py --skip-model-profile --run-locked-test --locked-allow-overwrite --package-type execution_ready
```

<!-- /2026-06-25-p0-p4-gpt-analysis-update -->

### 主要 Differences From Previous State
The 封包 manifest is now reproducibility-aware, locked-test post-processing is complete, 混合式決策 are explainable, 人工缺失 has conservative 回退, and CI summaries are generated.


<!-- 2026-06-20-邊緣端-profile-deferred -->

## 2026-06-20 決策：暫不執行 邊緣端估計

目前先不把 邊緣端/部署 latency profile 作為本輪主軸。主流程優先保留模型訓練、locked-test、規則/模型/混合式決策、缺失狀態 強健性、校準、特徵重要性 與 基準 結果；`model_profile_v74.py` 與 `results_v74/model_profile/` 先列為後續需要補強 IoMT 可部署性敘事時再單獨補跑的項目。

<!-- 2026-06-19-codex-update -->

## 2026-06-19 完整流程更新

`run_all_v74.py` 現在是主要完整流程入口。它會先建立 grouped locked-test split，再把 `--locked_split_manifest` 傳給 `train_v74.py`，因此主流程可以同時產生 no-support 與 locked-test no-support 輸出。

使用 project2 環境執行：
```powershell
& "C:\Users\ASUS\anaconda3\envs\project2\python.exe" run_all_v74.py --python "C:\Users\ASUS\anaconda3\envs\project2\python.exe"
```

主要預期輸出包含 `main_hybrid_summary.csv`、`main_hybrid_summary_locked_test.csv`、`three_way_conflict_summary.csv`、`ml_baseline_summary_5seed.csv`、`artificial_missingness_summary.csv` 與 `model_profile_summary.csv`。

<!-- /2026-06-19-codex-update -->


更新日期：2026-06-12  
適用版本：目前 v7.4 專案、正式輸出與 `all/` 同步狀態

本文件不是逐行 diff，而是整理目前版本相對原始版本的主要功能差異、資料處理差異、輸出差異與仍需注意的部分。

## 1. 整體差異總覽

| 類別 | 原始/早期狀態 | 目前狀態 |
|---|---|---|
| 資料單位 | 多處仍以 逐列 或左右耳同列處理 | Task1/Task2/Task3 都轉為 ear-level sample |
| Split | 較容易只看一般 train/valid | 使用 `case_id` group split，避免左右耳 leakage；另支援 locked split manifest |
| Task2 label | 混合 hearing type 與 high/low tone configuration | 僅保留 `WNL/SNHL/CHL/MHL` |
| Task2 特徵 | 曾使用 PTA/mean/slope 或整列左右耳 特徵 | 目前單耳 AC/BC/ABG 四頻率與 標記，共 32 個 |
| Task2 rule | 曾分散在 train/基準/dashboard | 集中於 `clinical_rules_v74.py` |
| Shared preprocessing | train、基準、dashboard 曾各自處理 | 新增 `preprocessing_v74.py` 共用主要轉換、證據狀態、警示 摘要 |
| Task3 規則 | 曾在不同位置判斷 NP/缺失 | 集中於 shared preprocessing / 臨床規則 流程 |
| 評估 | 主要 configured 驗證 | 新增 部署 no-support 評估 與 locked-test no-support 輸出 |
| Dashboard | 曾有自己的 rule / support 設定 | 目前固定 `support=None`，並顯示 警示 / 衝突 / 證據 |
| 基準 | 早期只有 forced rule 基準 | 新增 缺值感知 基準 與 傳統機器學習基準 |
| Error 分析 | 早期缺少逐筆 subgroup/error 輸出 | 新增 subgroup、衝突、Task2 臨床 focus |
| Split 實驗協定 | 早期缺少正式 manifest | 新增 `split_protocol_v74.py` |
| 人工缺失 | 無 | 新增缺失壓力測試 |
| Model profile | 無 | 新增參數量、checkpoint size、CPU latency |
| all 同步 | 需手動整理 | 新增 `sync_all_outputs_v74.py` |

## 2. 新增與修改的主要檔案

| 檔案 | 主要差異 |
|---|---|
| `train_v74.py` | 新增 ear-level preprocessing wrapper、no-support final 評估、逐筆預測列、reward 信心度、摘要_no_support aggregation、single-task 別名、locked-test no-support 評估、ablation preset。 |
| `preprocessing_v74.py` | 新增 shared preprocessing、模型資料列 builder、證據狀態、臨床警示 摘要。 |
| `clinical_rules_v74.py` | 新增 shared 臨床規則，集中 Task2/Task3 判斷與數值解析。 |
| `baseline_rules_v74.py` | 改為 缺值感知 基準，輸出 forced、covered-only、暫不判讀-as-error。 |
| `ml_baselines_v74.py` | 新增 傳統機器學習基準，並使用 shared preprocessing wrapper。 |
| `dashboard_three_tasks_metaIRL_v74.py` | 匯入 shared preprocessing / 警示，推論固定 `support=None`，輸出 rule/衝突/證據 欄位。 |
| `evaluate_v74.py` | 新增 no-support 與 locked-test no-support paper table export。 |
| `error_analysis_v74.py` | 新增 子群/錯誤分析、規則-模型衝突、Task2 臨床 subgroup、Task2 confusion focus。 |
| `split_protocol_v74.py` | 新增 grouped split / locked test manifest 腳本。 |
| `artificial_missingness_v74.py` | 新增人工缺失情境測試。 |
| `hybrid_evaluation_v74.py` | 新增 純神經網路 / rule / 混合式決策 比較。 |
| `model_profile_v74.py` | 新增參數量、checkpoint size、latency profile。 |
| `sync_all_outputs_v74.py` | 新增同步工具，將主要檔案與正式輸出複製到 `all/`。 |
| `mtl_meta_irl_transformer.py` | 修正 prototype/context 與不同 `d_model` 設定相容問題。 |

## 3. Task2 臨床規則 差異

目前 Task2 不再使用 PTA、`ac_mean`、`bc_mean`、`abg_mean` 做 hearing type rule。

目前使用：

- 500Hz
- 1000Hz
- 2000Hz
- 4000Hz

規則：

1. 任一有效頻率 `AC-BC >= 15` 視為 clear air-bone gap。
2. 有 ABG：
   - 任一有效 BC `> 25`：MHL
   - 否則：CHL
3. 無 ABG：
   - 任一有效 AC `> 25`：SNHL
   - 否則：WNL
4. BC 缺失 不視為 0，也不產生 fake ABG。
5. AC/BC NR 或 ABG 截尾 會標記為 uncertain 證據，降低 reward 信心度。

目前 Task2 rule 對 CSV：

| 指標 | 結果 |
|---|---:|
| prepared rows | 174 |
| label/rule mismatch | 0 |
| uncertain 證據 rows | 75 |

## 4. 特徵 差異

目前 union 特徵 數為 53。

| Task | 現在 特徵 | 差異 |
|---|---:|---|
| Task1 | 5 | 單耳 AC 四頻 + PTA |
| Task2 | 32 | 單耳 AC/BC/ABG 四頻率 + NR/缺失/截尾 標記 |
| Task3 | 16 | Tympanogram raw 數值 + real_zero/缺失_zero/np_zero 標記 |

Task2 已排除：

- `ac_mean`
- `bc_mean`
- `abg_mean`
- `abg_mean_censored`

原因：Task2 hearing type 主要是圖形/逐頻率判讀，不宜用平均值直接代表。

## 5. Loss 與 reward 差異

目前 loss：

```text
task_loss = exp(-log_var_task) * CE_loss
            + log_var_task
            + reward_weight * reward_loss
```

目前新增的重點：

- `reward_loss` 不再只是固定 target。
- `expert_consistency_target_and_confidence()` 會回傳 target 與 信心度。
- Task2 若 證據 缺失/NR/截尾，reward 信心度 下降。
- Task3 若 rule 證據 partial/缺失，reward 信心度 下降。

目的：讓 reward 作為輔助，不主導 CE loss。

## 6. 評估 差異

每個 run 現在會輸出：

| 檔案 | 說明 |
|---|---|
| `eval_summary.json` | configured 驗證，可能使用 meta support。 |
| `eval_summary_no_support.json` | 部署 no-support，固定 `support=None`。 |
| `eval_summary_locked_test_no_support.json` | 若使用 locked split manifest，輸出 locked test no-support。 |
| `per_class_metrics.csv` | configured per-class metrics。 |
| `per_class_metrics_no_support.csv` | no-support per-class metrics。 |
| `per_class_metrics_locked_test_no_support.csv` | locked test no-support per-class metrics。 |
| `prediction_rows.csv` | configured 逐筆預測。 |
| `prediction_rows_no_support.csv` | no-support 逐筆預測。 |
| `prediction_rows_locked_test_no_support.csv` | locked test no-support 逐筆預測。 |

run config 層級現在可彙整：

- `summary.csv`
- `summary_no_support.csv`
- `summary_locked_test_no_support.csv`
- `all_runs_metrics.csv`
- `all_runs_metrics_no_support.csv`
- `all_runs_metrics_locked_test_no_support.csv`

## 7. 新增獨立分析腳本

| 腳本 | 正式輸出位置 | 目前狀態 |
|---|---|---|
| `baseline_rules_v74.py` | `results_v74/rule_baselines_phase3/` | 已正式產生 |
| `ml_baselines_v74.py` | `results_v74/ml_baselines/` | 已正式產生 |
| `error_analysis_v74.py` | `paper_v74/error_analysis/` | 已正式產生 |
| `split_protocol_v74.py` | `results_v74/split_protocol/` | 已正式產生 |
| `artificial_missingness_v74.py` | `results_v74/artificial_missingness/` | 已正式產生 |
| `hybrid_evaluation_v74.py` | `paper_v74/hybrid_evaluation/` | 已正式產生 |
| `model_profile_v74.py` | `results_v74/model_profile/` | 已正式產生 |

## 8. Ablation 差異

目前新增 `ABLATION_RUN_CONFIGS`：

- `ablation_tiny_d32_l1_r010`
- `ablation_tiny_d32_l1_r000`
- `ablation_small_d64_l2_r010`

CLI 支援：

```powershell
python train_v74.py --config_preset recommended
python train_v74.py --config_preset ablation
python train_v74.py --config_preset all
```

single-task 支援：

```powershell
python train_v74.py --experiments single_task --single_task_target Task1
python train_v74.py --experiments single_task --single_task_target Task2
python train_v74.py --experiments single_task --single_task_target Task3
python train_v74.py --experiments single_task --single_task_target all
```

也可使用 aliases：

- `single_task_task1`
- `single_task_task2`
- `single_task_task3`

## 9. 目前仍需注意

| 項目 | 狀態 |
|---|---|
| `artificial_missingness` 的 `scenario=none` | 是全資料 inference，不是 正式 驗證 split。 |
| Task2 CHL | 只有 2 筆，macro-F1 對 split 很敏感。 |
| Task3 C | 樣本少，需搭配 per-class 與 subgroup。 |
| rule 基準 | forced rule 很高，但 缺值感知 暫不判讀-as-error 更適合作為缺值情境對照。 |
| `all.zip` | 仍是舊壓縮檔，不代表目前最新版 `all/`。 |

## 10. 建議下一步

1. 修正所有報告或簡報中會誤解的指標標示，尤其 `0.984` 應標為 artificial-缺失狀態 full-資料 `scenario=none`。
2. 論文主結果使用 `paper_v74/tables/summary_no_support_all_configs.csv`。
3. 針對 Task2 CHL、Task3 C 報告 support 與 per-class F1。
4. 若要更嚴謹比較 混合式決策，補 split-aligned 混合式決策 比較。
5. 若要投論文，補 locked-test 正式完整訓練結果與 信心度 interval。

---

## 2026-06-18 新增差異摘要

本輪相對於先前版本新增/修改：`calibration_analysis_v74.py`、`run_locked_test_v74.py`、`run_all_v74.py`、`sync_all_outputs_v74.py`、`error_analysis_v74.py`、`evaluate_v74.py`、`hybrid_evaluation_v74.py`、`model_profile_v74.py`、`dashboard_three_tasks_metaIRL_v74.py`、`preprocessing_v74.py`。核心差異是從 neural-architecture-only 敘事，轉為 規則優先 混合式決策 support，並補上 校準、三方衝突、low support 警示、部署 profile 與 locked-test 安全入口。

注意：`backups/` 與 `outputs/phase11_check/` 保留歷史快照，不代表目前正式狀態；正式狀態以根目錄檔案、保留的說明文件、`paper_v74/`、`results_v74/` 與 `run_all_v74.py` 為準。

## 2026-06-21 P0-P2 差異摘要

此歷史段落已整理為可讀摘要：當時新增 `checkpoint_utils_v74.py` 的 checkpoint resolver、`feature_importance_v74.py` 的 特徵群組 permutation importance 與 inference-time ablation、`artificial_missingness_v74.py` 的 degradation/per-class/證據 compensation、`hybrid_evaluation_v74.py` 的 信心度-gated 混合式決策 與 低信心度 暫不判讀 metrics、`run_all_v74.py` 的 特徵重要性 與 clean all/ sync，以及 `sync_all_outputs_v74.py` 的 封包 inventory manifest。

## 2026-06-21 RUN_CONFIGS / ABLATION_RUN_CONFIGS 差異摘要

此歷史段落已整理為可讀摘要：`RUN_CONFIGS` 從早期較少組合擴充到 15 組，`ABLATION_RUN_CONFIGS` 擴充到 5 組，並加入 `MODEL_SIZE_CONFIGS`、`MISSING_AUG_PROFILES` 與 `make_run_config()`。目前這些設定已集中在 `train_v74.py 內建 RUN_CONFIGS/ABLATION_RUN_CONFIGS`，並以 `舊分工機制（已移除）` JSON 支援 A/B 分散式訓練。

<!-- 2026-06-27-p0-p4-final-alignment -->

## 2026-06-27 P0-P4 收斂後最新說明

### 最新研究定位
本專案目前應定位為：**Missing-aware clinical-rule-guided hybrid decision-support framework for ear-level hearing classification under incomplete audiological evidence**。核心不是單純證明 Transformer 或 MetaIRL 分數較高，而是證明在不完整聽力學資料下，系統能結合臨床規則、模型預測、規則/模型衝突警示、低信心度暫不判讀與 locked-test 可追溯性。

### 目前新增或強化的證據鏈
- `ml_baseline_locked_test_summary_5seed.csv`：補齊 classical ML 在 locked-test 上的公平比較。
- `main_hybrid_summary*.csv`：新增 rule availability、rule failure、rule correction、model fallback success、rule-model conflict、warning rate 等臨床價值指標。
- `hybrid_threshold_sweep*.csv`：用 threshold sweep 說明 confidence gate 不是任意指定。
- `hybrid_strategy_mcnemar*.csv`：提供 strategy-level paired comparison / McNemar 檢定。
- `clinical_error_taxonomy_summary.csv`：將 true label / rule label / model prediction 整理成臨床錯誤 taxonomy。
- `calibration_policy_summary.csv`：把 confidence threshold curve 收斂成可引用的 policy summary。
- `statistical_summary_all_configs.csv`：新增 bootstrap CI 欄位。
- `artificial_missingness_v74.py` 與 `feature_importance_v74.py`：主流程預設改用 `run_13_tiny_m15_bc_dominant_r010/no_meta_seed_*` 作為 primary 5-seed 分析基礎。

### 論文敘事應避免過度主張
- 不應主張 device shift 已完成驗證，除非後續補上 device label 或 cross-device validation。
- 不應主張 Transformer / MetaIRL / IRL 本身是唯一主要貢獻；這些應寫成 neural component 或 ablation factor。
- 不應主張 hybrid 一定全面擊敗 clinical rule；應主張 hybrid 的價值在 rule coverage、model fallback、conflict warning 與 incomplete-data tolerance。
- 邊緣端 latency/profile 目前可作補充，不作本輪主軸；若要寫 IEEE IoT/IoMT，需以概念性 IoMT decision-support pipeline、missingness robustness 與 clinical warning 為主。

### 建議論文主線
1. Subject-level / locked-test traceable split 建立可信評估基礎。
2. Clinical rule baseline 建立可解釋診斷參考。
3. Neural model 補足 rule 無法完整判斷或資料不完整的案例。
4. Hybrid rule-guided framework 整合 rule decision、model fallback、warning 與 abstention。
5. Missingness robustness、feature importance、calibration 與 statistical tests 支撐系統可靠性。

<!-- /2026-06-27-p0-p4-final-alignment -->

<!-- 2026-07-01-task1-missingness-alignment -->
## 2026-07-01 Task1 Artificial Missingness Alignment

本次差異重點是補齊 Task1 的人工缺失壓力測試。原本 Task1 在訓練端已有 `mask_pta`、`mask_high_freq`、`mask_low_freq`、`mask_all_ac`，但 `artificial_missingness_v74.py` 分析端只有 `none`，導致訓練設定與 robustness analysis 不一致。現在已新增 `task1_no_pta`、`task1_no_high_freq`、`task1_no_low_freq`、`task1_no_all_ac`，並在 manifest 中列出。

`gpt_suggestions_action_plan_v74.md` 與 `程式碼與結果改善建議報告.md` 已移除，不再作為正式狀態依據。正式狀態以根目錄程式碼、保留的說明文件、`paper_v74/`、`results_v74/`、`run_all_v74.py` 與 `sync_all_outputs_v74.py` 為準。
<!-- /2026-07-01-task1-missingness-alignment -->
## 2026-07-02 最新規則更新：Rule Evidence Score 與 Hybrid Gating

- Task2 clear ABG 現在定義為 `AC-BC >= 15 dB`。
- Task2 borderline ABG 現在定義為 `10 <= AC-BC < 15 dB`；borderline 不直接觸發 CHL/MHL，只扣 rule evidence score 0.1 並加上 `abg_borderline` warning。
- Task2 的 `rule_forced` 仍保留硬判 label；但正式 rule-first / hybrid 是否採用 rule，改由 `rule_confidence` / `rule_evidence_score` 門檻控制。
- Task2 score 起始為 1.0：缺 core AC 扣 0.15；6000/8000 兩者都缺扣 0.05；BC 部分缺失整組扣 0.3；no BC data 直接壓到 0.5；NR/censored 只加 warning，不當 missing。
- Task3 evidence score 納入 `tymp_Vea` 缺失檢查；Vea 缺失只扣 0.05 並加 warning，不直接改 A/B/C label。
- Task3 `peak_daPa` 缺失時 score 最高 0.5；`peak NP + compliance NP` 視為有效 B 型證據；`-300 < peak_daPa <= -150` 為 C 區間但扣 0.2，並保留 C/B compatible label。
- Hybrid rule-first 現在使用 score gating：`rule_confidence >= 0.8` 且 rule label 存在時採用 rule；score 不足時 fallback model；model confidence 低於門檻時可輸出 `INSUFFICIENT_EVIDENCE`。
- 新增或保留的輸出欄位包含 `rule_evidence_score`、`score_deductions`、`rule_confidence`、`warning_reasons`、`hybrid_decision_reason`。
- 注意：依目前資料暫存檢查，`AC-BC>=15` 搭配「任一頻率有 clear ABG 即判 CHL/MHL」會明顯拉低 Task2 rule-forced 表現；這是規則定義的結果，不是流程錯誤，後續若要改善需再討論是否加入「至少兩個非 censored ABG 頻率」等條件。
## 2026-07-02 與前一版輸出設計差異

本輪差異不是新增模型架構，而是新增 paper-ready analysis layer：
- hybrid_evaluation_v74.py 從單純比較策略分數，擴充為 rule contribution、hybrid explainability、method comparison 三種正式表。
- artificial_missingness_v74.py 從性能壓力測試，擴充為缺失情境下的 hybrid reason/fallback 分析。
- feature_importance_v74.py 從原始 ablation/permutation 輸出，擴充為 feature group summary 與 feature-missingness link。
- calibration_analysis_v74.py 從 raw calibration outputs，擴充為可直接放入論文的 calibration_summary_paper。
- run_all_v74.py 已將上述輸出納入完整流程與 verify_key_outputs()，避免產出不完整卻未被發現。

研究主張差異：不再把 Transformer/MetaIRL 寫成核心貢獻，而是把貢獻收斂為 clinical-rule-guided hybrid decision support under incomplete audiological evidence。

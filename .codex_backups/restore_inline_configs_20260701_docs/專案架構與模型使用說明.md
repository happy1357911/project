# 專案架構與模型使用說明

<!-- 2026-06-27-narrative-alignment -->

## 2026-06-27 文字敘事與命名收斂

本段為目前對外說明的優先採信版本。後續給 GPT、教授或論文草稿分析時，請先以本段理解研究定位；較早日期段落若仍出現舊名稱或舊數字，只作歷史紀錄，不作為目前主張。

目前研究主軸不是單純的神經網路架構或模型準確率比較，而是：

```text
Missing-aware clinical-rule-guided hybrid decision-support framework
for ear-level hearing classification under incomplete audiological evidence.
```

中文定位：聽力師規則引導、缺值感知的混合式聽力輔助判讀系統。

目前敘事原則：
- `MetaIRL`、Transformer、prototype/meta-learning 只作為神經網路元件或消融模組，不作為唯一主貢獻。
- 主要臨床問題是 Task2/Task3 在缺失、NR/截尾值、ABG 邊界值、無 peak / 無 tympanogram 等不完整證據下的可靠輔助判讀。
- 混合式決策的價值是規則優先、模型回退、警示、暫不判讀與規則-模型衝突透明化；不可寫成混合式決策已全面高於規則決策。
- 傳統機器學習基準很強；若實驗協定不同，只能說是具競爭力的 grouped validation 基準，不能直接與 locked-test 神經網路結果作公平勝負結論。
- 人工缺失分析目前是強健性壓力測試證據；除非對齊主要設定與 5 seeds，否則不要寫成最終主要強健性結果。
- IoMT / 部署目前應寫成概念性臨床決策輔助流程；邊緣端 latency/profile 不是本輪主軸，device-shift 主張需等有 device-level 驗證後再放入。
- `all/` 是給 GPT/教授分析的封包；可包含目前使用的原始資料，但沒有 checkpoint 與大型逐列預測時，仍不是完整可重跑的 execution-ready 封包。

<!-- /2026-06-27-narrative-alignment -->

<!-- 2026-06-26-current-architecture-alignment -->

## 2026-06-26 目前專案架構對齊

本段是目前架構的優先採信版本；舊段落保留作為歷史紀錄。
目前程式碼對齊重點：
- Task1 資料來源：`task1_all_three_common14_v1.csv`。
- Task2/Task3 資料來源：`task2_3_pure_data(6_24).xlsx`。
- 目前特徵數：Task1 = 5、Task2 = 36、Task3 = 16、三任務 union = 53。
- Task2 模型輸入包含 AC 500/1000/2000/4000/6000/8000 Hz、六頻 AC NR 標記、BC 500/1000/2000/4000 Hz、BC NR/缺失標記、ABG 500/1000/2000/4000 Hz 的數值/缺失/截尾標記。
- Task2 規則使用 AC 500/1000/2000/4000 Hz，加上 6000/8000 Hz 至少一個高頻存在作為完整性條件；ABG 以 `abs(AC-BC)>10 dB` 判定，8-10 dB 只作邊界警示。
- Task3 規則：`peak_daPa <= -300` 判 B，`-300 < peak_daPa <= -150` 判 C 且信心度較低，`peak_daPa > -150` 判 A；NP peak 加 NP compliance 判 B。
- 混合式決策的規則優先策略只有在 `complete_for_rule=True`、`baseline_covered=True` 且 `rule_confidence` 達門檻時採用規則；否則使用模型，若模型信心度低於門檻則可暫不判讀。
- `configs/run_configs_v74.json` 為完整 20 組設定：15 組 `run_configs` 加 5 組 `ablation_run_configs`。
- `configs/splits/run_configs_v74_part_A.json` 與 `configs/splits/run_configs_v74_part_B.json` 各選 10 組，供兩台電腦分工訓練；沒有 A/B 自動回退。
- `all/` 預設是給 GPT/教授分析的 `analysis_only` 封包；可用 `--include-raw-data` 納入目前使用的原始資料，但未包含 checkpoint 與大型逐列預測時，仍不是完整可重跑封包。
- 本輪邊緣端/model profile 仍可用 `--skip-model-profile` 延後，不作為目前主流程必跑項。

目前核心模組分工：

| 模組 | 目前角色 |
|---|---|
| `preprocessing_v74.py` | 三任務共用資料載入、ear-level 展開、缺失/NR/NP/zero 標記、臨床警示摘要。 |
| `clinical_rules_v74.py` | Task1/Task2/Task3 規則決策、規則信心度、covered/完整證據、compatible labels。 |
| `train_v74.py` | 主訓練、JSON config loader、`v1_split` 分散式 config、缺失 augmentation、locked-test no-support 評估。 |
| `baseline_rules_v74.py` | 臨床規則 only 基準與規則預測。 |
| `ml_baselines_v74.py` | Logistic Regression、Random Forest、HistGradientBoosting 與可選 boosting 基準。 |
| `hybrid_evaluation_v74.py` | 規則/模型/混合式決策比較、信心度 gate、決策原因摘要。 |
| `error_analysis_v74.py` | true label / 規則 label / 模型預測三方衝突、決策安全性摘要。 |
| `evaluate_v74.py` | paper table/figure export、per-class low-support table、repeated-seed 統計摘要。 |
| `artificial_missingness_v74.py` | complete/no-tymp/no-peak/no-BC/NR/邊界 ABG 等缺失壓力測試。 |
| `feature_importance_v74.py` | 特徵群組 permutation importance 與 inference-time ablation，不是重新訓練 ablation。 |
| `sync_all_outputs_v74.py` | `analysis_only` / `execution_ready` 封包同步與 manifest 記錄。 |

投稿定位建議仍維持：主軸應是缺值感知、臨床規則引導、locked-test-traceable IoMT 決策輔助流程，而不是只主打 Transformer 或 MetaIRL。

<!-- /2026-06-26-current-architecture-alignment -->

<!-- 2026-06-25-p0-p4-gpt-analysis-update -->

## 2026-06-25 P0-P4 GPT 分析回應更新

本次更新是在核對目前程式碼、最新版 GPT all.zip 分析與教授回饋後完成。目標是讓專案從只看模型分數的敘事，收斂為缺值感知、臨床規則引導、可追溯 locked-test 的決策輔助流程。

已完成更新：
- `sync_all_outputs_v74.py` 現在會標示封包類型：`analysis_only`、`execution_ready` 或 `custom`。
- sync manifest 會明確記錄是否包含原始資料、checkpoint 與大型逐列預測檔案。
- `run_all_v74.py` 會把封包旗標傳給同步流程，並驗證 `sync_manifest_v74.json` 是否符合執行參數。
- `run_locked_test_v74.py` 現在會產生 locked-test error analysis、三方衝突表、校準摘要、ECE bins、Brier score 與信心度門檻 curves。
- `run_all_v74.py` 現在會建立 `results_v74/calibration_analysis_locked_test/`，並用 `--mode all` 執行 error analysis。
- 混合式決策輸出現在包含 `hybrid_decision_reason`、`hybrid_warning_reasons` 與決策原因摘要 CSV。
- 人工缺失現在會針對 incomplete 或 corrupted stress scenario 採用較保守的 scenario-level 模型回退。
- Task2 artificial ABG 邊界 stress 現在使用 10 dB 而不是 15 dB，使其代表邊界而非明確 ABG。
- Error analysis 現在會輸出 `decision_safety_summary.csv`。
- Dashboard 現在會顯示模型信心度、混合式決策原因與低信心度暫不判讀。
- `evaluate_v74.py` 現在會輸出 `statistical_summary_all_configs.csv`，包含 mean、std、SEM 與 clipped 95% CI。

建議正式刷新指令：

```powershell
python run_all_v74.py --skip-model-profile --run-locked-test --locked-allow-overwrite --package-type analysis_only
```

只有在需要可重跑的大型封包時才使用：

```powershell
python run_all_v74.py --skip-model-profile --run-locked-test --locked-allow-overwrite --package-type execution_ready
```

<!-- /2026-06-25-p0-p4-gpt-analysis-update -->

### 投稿定位提醒
主要貢獻應寫成 臨床規則引導 缺值感知 決策輔助。MetaIRL/Transformer 不應作為唯一貢獻主張；Random Forest 與 GBDT-style 方法必須誠實視為強基準。


<!-- 2026-06-25-rule-source-update -->

## 2026-06-25 規則與資料來源更新

Task2/Task3 流程的正式資料來源已統一為 `task2_3_pure_data(6_24).xlsx`。這是 6/24 修正版，已透過 shared preprocessing 路徑供 preprocessing、訓練、ML baseline、rule baseline、split protocol、人工缺失、特徵重要性、混合式決策評估輸入，以及 dashboard/model-row logic 使用。

規則更新：
- `xxNR` 視為已測量但被截尾的證據，會替換成該頻率的設備上限值，並在規則完整性判定中視為完整；純 `NR` token 不視為有效 numeric NR 數值。
- Task2 模型輸入現在包含 AC 500/1000/2000/4000/6000/8000 Hz，以及六個頻率的 AC NR 標記；ABG 仍維持 500/1000/2000/4000 Hz。
- Task2 規則以 `abs(AC-BC) > 10 dB` 判定 ABG，並用 500/1000/2000/4000/6000/8000 Hz 檢查 AC 是否異常；6000/8000 Hz 只要其中一個存在，就視為高頻 AC 完整。
- Task2 的 8-10 dB ABG 只作邊界警示，不會單獨觸發 CHL/MHL；除非其他頻率已有明確 ABG。
- Task3 規則以 peak 為主：`peak_daPa > -150 => A`，`-300 < peak_daPa <= -150 => C` 且信心度 0.6，`peak_daPa <= -300 => B`；NP peak/compliance 證據仍判為 Type B。
- 混合式決策的規則優先策略只有在 `complete_for_rule=True`、`baseline_covered=True` 且規則信心度達到設定門檻時使用規則，其他情況交由模型處理。

本次更新後的 smoke check：
- `python run_all_v74.py --compile-only` 通過所有根目錄 Python 檔案。
- `python run_all_v74.py --dry-run --skip-model-profile --run-locked-test --locked-allow-overwrite` 顯示完整流程順序。
- `baseline_rules_v74.py` 使用修正版資料來源時，產生 Task2 n=382 與 Task3 n=270。

<!-- /2026-06-25-rule-source-update -->

<!-- 2026-06-25-external-run-configs -->

## 2026-06-25 外部 run config 更新

訓練參數組合已獨立到 `configs/run_configs_v74.json`。之後若要調整 15 組 recommended 或 5 組 ablation，不需要直接修改 `train_v74.py` 的常數區塊。

JSON 內容包含：
- `model_size_configs`：base / small / tiny model dimensions。
- `missing_aug_profiles`：缺失 augmentation 機率，以及 Task1/Task2/Task3 strategy weights。
- `run_configs`：15 組建議使用的 masked configurations。
- `ablation_run_configs`：5 組 ablation configurations。

預設用法：
```powershell
python run_all_v74.py --run-config-file configs/run_configs_v74.json --skip-model-profile --run-locked-test --locked-allow-overwrite
```

`train_v74.py`、`run_all_v74.py` 與 `run_locked_test_v74.py` 都支援外部 config file。`sync_all_outputs_v74.py` 也會把 `configs/run_configs_v74.json` 同步到 `all/`。

<!-- /2026-06-25-external-run-configs -->

<!-- 2026-06-20-邊緣端-profile-deferred -->

## 2026-06-20 決策：暫不執行 邊緣端估計

目前先不把 邊緣端/部署 latency profile 作為本輪主軸。主流程優先保留模型訓練、locked-test、規則/模型/混合式決策、缺失狀態 強健性、校準、特徵重要性 與 基準 結果；`model_profile_v74.py` 與 `results_v74/model_profile/` 先列為後續需要補強 IoMT 可部署性敘事時再單獨補跑的項目。

<!-- 2026-06-19-codex-update -->

## 2026-06-19 完整流程更新

`run_all_v74.py` 現在是主要完整流程入口。它會先建立 grouped locked-test split，再把 `--locked_split_manifest` 傳給 `train_v74.py`，因此主流程可以同時產生 no-support 與 locked-test no-support 輸出。

使用 project2 環境執行：
```powershell
& "C:\Users\ASUS\anaconda3\envs\project2\python.exe" run_all_v74.py --python "C:\Users\ASUS\anaconda3\envs\project2\python.exe"
```

主要預期輸出包含 `main_hybrid_summary.csv`、`main_hybrid_summary_locked_test.csv`、`three_way_conflict_summary.csv`、`ml_baseline_summary_5seed.csv`、`artificial_missingness_summary.csv` 與 `model_profile_summary.csv`。

<!-- /2026-06-19-codex-update -->


更新日期：2026-06-12  
適用版本：目前根目錄 v7.4 程式、正式輸出與 `all/` 同步狀態

## 1. 專案目標

本專案目前的核心目標不是單純證明模型比臨床規則更準，而是建立一個能處理不完整聽力資料的 ear-level multi-task learning 流程。

目前資料中會出現：

- 缺失 數值
- AC/BC NR
- ABG 截尾 證據
- tympanogram NP
- 左右耳 label / 特徵 需要拆開訓練

目前較合理的研究定位是：

```text
Clinically guided ear-level multi-task hearing classification
under missing / NR / censored audiological measurements.
```

論文主結果建議以 no-support / support-free 驗證 與 locked-test no-support 為主，因為 dashboard 實際推論固定使用 `support=None`。configured 驗證 可保留為 development 驗證 或 ablation。

## 2. 主要程式與功能

| 檔案 | 功能 |
|---|---|
| `train_v74.py` | 主訓練流程、單耳層級轉換、group split、loss、final 評估、摘要 aggregation、single-task 別名、locked-test no-support 評估。 |
| `mtl_meta_irl_transformer.py` | Neural multi-task component，包含 tokenizer、Transformer encoder、task embedding、FiLM adapter、classification heads、reward heads；prototype/meta-learning 僅作 消融模組。 |
| `preprocessing_v74.py` | 共用 Task1/Task2/Task3 preprocessing、模型資料列 builder、證據狀態、臨床警示 摘要。 |
| `clinical_rules_v74.py` | 共用臨床規則。Task2 聽損類型規則、Task3 tympanogram 規則、NR/缺失/截尾 數值解析集中在此。 |
| `baseline_rules_v74.py` | 缺失-aware 臨床規則 基準，輸出 forced、covered-only、暫不判讀-as-error 指標。 |
| `ml_baselines_v74.py` | 傳統機器學習基準，支援 Logistic Regression、Random Forest、HistGradientBoosting、MLP，optional boosting 缺套件時會記錄 略過。 |
| `evaluate_v74.py` | 將訓練結果匯出成 paper tables，包含 configured、no-support、locked-test no-support。 |
| `error_analysis_v74.py` | Subgroup/error 分析、規則-模型衝突、Task2 臨床 subgroup、Task2 confusion focus。 |
| `split_protocol_v74.py` | 可重現 grouped split protocol 與 locked test manifest。 |
| `artificial_missingness_v74.py` | 人為缺失測試，用來觀察 no-support model 在指定缺值情境下的穩定性。 |
| `hybrid_evaluation_v74.py` | 比較 純神經網路、rule-forced、rule-暫不判讀-as-error、混合式決策-規則優先，並輸出 規則/模型 分工與 警示。 |
| `model_profile_v74.py` | 輸出 model parameter count、checkpoint size、CPU latency。 |
| `dashboard_three_tasks_metaIRL_v74.py` | Streamlit dashboard，推論固定 `support=None`，並顯示 規則 label、證據狀態、警示、規則-模型衝突。 |
| `sync_all_outputs_v74.py` | 將主要程式、md、CSV 與正式輸出同步到 `all/`。 |

## 3. 輸入資料

| Task | CSV | 單位 | 目的 |
|---|---|---|---|
| Task1 | `task1_all_three_common14_v1.csv` | ear-level | 以 AC/PTA 判斷聽損程度。 |
| Task2 | `task2_3_pure_data(6_24).xlsx` | ear-level | 以 AC/BC/ABG 與 標記 判斷 WNL/SNHL/CHL/MHL；目前與 Task3 共用來源檔。 |
| Task3 | `task2_3_pure_data(6_24).xlsx` | ear-level | 以 tympanogram 判斷 A/B/C；目前與 Task2 共用來源檔。 |

目前三個任務都會將左右耳拆成獨立 sample，並使用 `case_id` 進行 group split，避免同一個人的左右耳同時出現在 train 與 valid。

## 4. 目前模型輸入 特徵

目前 union 特徵 數為 53。

| Task | 特徵 count | 特徵 摘要 |
|---|---:|---|
| Task1 | 5 | `ac_500Hz`, `ac_1000Hz`, `ac_2000Hz`, `ac_4000Hz`, `ac_PTA` |
| Task2 | 36 | AC 六頻、AC NR 標記 六頻、BC 四頻、BC NR 標記、BC 缺失標記、ABG 四頻、ABG 缺失標記、ABG 截尾 標記 |
| Task3 | 16 | Tympanogram raw 數值，加上 real_zero / 缺失_zero / np_zero 標記 |

Task2 已移除 `ac_mean`、`bc_mean`、`abg_mean` 這類平均特徵，避免把圖形判讀問題誤簡化成平均值判斷。

## 5. Task2 臨床規則

Task2 rule 目前集中於 `clinical_rules_v74.py`。

標準 label：

- `WNL`
- `SNHL`
- `CHL`
- `MHL`

已排除：

- `high tone loss`
- `low tone loss`
- `high and low tone loss`
- 空值或非標準 hearing type

核心判斷邏輯：

1. 只看 500/1000/2000/4000Hz。
2. 逐頻率判斷，不使用 PTA、`ac_mean`、`bc_mean`、`abg_mean` 作為 hearing type rule。
3. 若任一有效頻率有 `ABG > 10`，代表有 air-bone gap。
4. 有 ABG 時：
   - 任一有效 BC `> 25`：`MHL`
   - 否則：`CHL`
5. 沒有 ABG 時：
   - 任一有效 AC `> 25`：`SNHL`
   - 否則：`WNL`
6. BC 缺失 不會被當成 0，也不會產生 fake ABG。
7. 若 證據 有 缺失/NR/截尾，reward 信心度 會下降。

目前重新檢查 Task2 CSV：

| 指標 | 結果 |
|---|---:|
| prepared Task2 standard ear-level rows | 384 |
| label/rule mismatch | 27 |
| 主要 mismatch contexts | abg_邊界 / no_bc_資料 / 缺失證據 |
| note | Task2 labels outside WNL/SNHL/CHL/MHL are excluded from standard Task2 訓練/評估. |

## 6. 模型與 loss

模型由 `mtl_meta_irl_transformer.py` 定義。

主要元件：

- Numeric特徵Tokenizer
- TransformerEncoder
- task embedding
- FiLMAdapter
- classification heads
- reward heads
- task uncertainty `log_vars`
- prototype meta-learning support/query mechanism

每個 task 的 loss：

```text
task_loss = exp(-log_var_task) * CE_loss
            + log_var_task
            + reward_weight * reward_loss
```

其中：

- `CE_loss`：class-weighted CrossEntropyLoss。
- `reward_loss`：reward head 與 expert consistency target 的 MSE。
- `reward_confidence`：對 缺失/NR/截尾 或不完整 rule 證據 降低 reward MSE 權重。
- `reward_weight`：控制 reward loss 對總 loss 的影響。

## 7. 實驗模式

| Mode | 說明 |
|---|---|
| `full` | Task1/Task2/Task3，使用 CE + reward + meta-learning support/prototype 路徑。 |
| `no_meta` | Task1/Task2/Task3，不使用 meta support/prototype。 |
| `no_irl` | Task1/Task2/Task3，`reward_weight=0`。 |
| `single_task` | 單任務訓練。可用 `--single_task_target Task1|Task2|Task3|all` 控制目標。 |
| `single_task_task1` | Task1-only alias。 |
| `single_task_task2` | Task2-only alias。 |
| `single_task_task3` | Task3-only alias。 |

## 8. RUN_CONFIGS 與 ablation

目前訓練參數不再以 Python 內寫死的少數 `RUN_CONFIGS` 為主，而是外部化到：

```text
configs/run_configs_v74.json
```

目前 JSON 結構：

| 類別 | 數量 | 意義 |
|---|---:|---|
| `model_size_configs` | 3 | base / small / tiny 神經網路元件 容量設定。 |
| `missing_aug_profiles` | 5 | 不同程度的 訓練 masking / 缺失狀態 augmentation profile。 |
| `run_configs` | 15 | 正式 建議設定，覆蓋 model size、BC-dominant、tymp-dominant、heavy masking 等設定。 |
| `ablation_run_configs` | 5 | 消融實驗 configs，仍保留不同程度的 masking，只改 reward、schedule 或 support 相關設定。 |

兩台電腦分工時使用：

```text
configs/splits/run_configs_v74_part_A.json
configs/splits/run_configs_v74_part_B.json
```

A/B split 只負責選擇 config 子集合；完成後需合併 `results_v74_part_A/five_runs/*` 與 `results_v74_part_B/five_runs/*` 到主電腦 `results_v74/five_runs/`，再執行後處理、evaluate、混合式決策、校準、缺失狀態、特徵重要性 與 sync。

## 9. 正式輸出狀態

目前已正式產生並同步到 `all/` 的主要輸出：

| 類別 | 位置 |
|---|---|
| Paper tables | `paper_v74/tables/` |
| No-support paper tables | `paper_v74/tables/summary_no_support_all_configs.csv`、`all_runs_no_support_all_configs.csv`、`per_class_no_support_all_configs.csv` |
| 缺失-aware rule 基準 | `results_v74/rule_baselines_phase3/` |
| 傳統機器學習基準 | `results_v74/ml_baselines/` |
| Error / subgroup 分析 | `paper_v74/error_analysis/` |
| 人工缺失 | `results_v74/artificial_missingness/` |
| Model profile | `results_v74/model_profile/` |
| 混合式決策 評估 | `paper_v74/hybrid_evaluation/` |
| Split 實驗協定 | `results_v74/split_protocol/` |
| all 同步 manifest | `all/sync_manifest_v74.json` |

`all/` 目前可用於 GPT 做程式與結果分析；但舊的 `all.zip` 是 2026-06-08 的舊壓縮包，不應視為最新版。

## 10. 目前重要正式結果

### 缺值感知 rule 基準

| Task | n_total | coverage | 暫不判讀_rate | 暫不判讀_as_error_accuracy | 暫不判讀_as_error_macro_f1 |
|---|---:|---:|---:|---:|---:|
| Task1 | 9738 | 1.0000 | 0.0000 | 1.0000 | 1.0000 |
| Task2 | 382 | 0.3874 | 0.6126 | 0.8508 | 0.8130 |
| Task3 | 270 | 1.0000 | 0.0000 | 0.9889 | 0.9531 |

### 人工缺失分析

`scenario=none` 是全資料 no-support inference，不等同 正式 驗證 split。Task2 在 `scenario=none` 的 macro-F1 為 0.9835，但正式論文主表仍應優先看 驗證 no-support tables。

### 模型 profile

目前 profile 使用 `run_01_rr_k1_anchor_r015/full_seed_0/best_model.pth`：

| 指標 | 數值 |
|---|---:|
| parameter_count | 1,016,496 |
| checkpoint_size_mb | 3.93 |
| CPU latency batch=1 | 約 2.96 到 3.43 ms |
| CPU latency batch=32 | 約 16.56 到 17.86 ms |
| CPU latency batch=512 | 約 173.40 到 250.91 ms |

## 11. 常用指令

```powershell
python train_v74.py --data_dir . --results_dir results_v74
```

```powershell
python evaluate_v74.py --results_dir results_v74 --paper_dir paper_v74
```

```powershell
python baseline_rules_v74.py --data-dir . --output-dir results_v74/rule_baselines_phase3
```

```powershell
python ml_baselines_v74.py --data-dir . --output-dir results_v74/ml_baselines --seeds 0,1,2,3,4
```

```powershell
python error_analysis_v74.py --results_dir results_v74 --output_dir paper_v74/error_analysis --mode both
```

```powershell
python split_protocol_v74.py --data_dir . --output_dir results_v74/split_protocol --seeds 0,1,2,3,4 --locked_test_ratio 0.2
```

```powershell
python hybrid_evaluation_v74.py --results_dir results_v74 --rule_predictions results_v74/rule_baselines_phase3/rule_baseline_predictions.csv --output_dir paper_v74/hybrid_evaluation --mode no_support --rule_confidence_threshold 0.7
```

```powershell
conda run -n project2 python artificial_missingness_v74.py --data-dir . --checkpoint results_v74/five_runs/run_01_rr_k1_anchor_r015/full_seed_0/best_model.pth --output-dir results_v74/artificial_missingness --tasks Task1,Task2,Task3 --batch-size 512 --device cpu
```

```powershell
conda run -n project2 python model_profile_v74.py --checkpoint results_v74/five_runs/run_01_rr_k1_anchor_r015/full_seed_0/best_model.pth --output-dir results_v74/model_profile --device cpu --batch-sizes 1,32,512 --warmup 3 --repeats 10
```

```powershell
python sync_all_outputs_v74.py
```

## 12. 論文解讀注意事項

1. Task2 label 與 臨床規則 高度相關，因此不宜把主張寫成 神經網路模型 已全面高於 規則決策；應強調 不完整證據 下的 規則優先 / model-回退 決策輔助。
2. 更合理的主軸是：
   - 缺失/NR/截尾 證據 下的穩定性
   - no-support 部署 performance
   - multi-task 是否輔助少樣本任務
   - reward 信心度 如何避免 rule 過度主導 CE loss
3. CHL 只有 2 筆，Task2 macro-F1 必須搭配 per-class recall/F1 解讀。
4. Task3 B/C 樣本少，應搭配 子群/錯誤分析 檢查 NP/缺失標記 的作用。
5. `full` 模式中的 support/prototype 屬於 configured 驗證 / ablation；dashboard 實際使用時應看 no-support。

---

## 2026-06-18 最新狀態更新

本專案目前已從單純 神經網路架構 敘事，收斂為 **缺值感知、臨床規則引導、規則優先 混合式決策 hearing 決策輔助 framework**。已完成 P0/P1 程式修正與 smoke 驗證：Task2 `abg_borderline` 信心度 修正、rule 基準 / 混合式決策 主表、三方衝突 error 分析、dashboard 規則/模型/混合式決策 顯示、low-support 警示、校準 分析、混合式決策/GBDT 部署 profiling，以及 locked-test 獨立 runner。

仍需明確區分：`run_locked_test_v74.py` 與 `run_all_v74.py --run-locked-test` 已提供正式 locked-test 入口，但完整 locked-test 全量訓練結果尚未長跑產生；device-shift claim 仍需資料欄位或外部 metadata 支撐，否則論文題目與摘要不應主張已驗證 device shift。

## 2026-06-21 IoMT 強健性-oriented 流程

此歷史段落已整理為可讀摘要：當時已將研究方向收斂為 缺值感知、臨床規則引導 IoMT 決策輔助 流程。核心流程包含 ear-level shared preprocessing、grouped/locked split、neural multi-task model、臨床規則 基準、傳統機器學習基準、true/規則/模型 三方衝突、規則優先 混合式決策、incomplete 資料 強健性、特徵群組 importance，以及 all/ 封包 可追溯性。

目前最新狀態仍以本文件最上方 2026-06-26 區塊為準；邊緣端/profile 不是本輪必跑主軸，重點是 incomplete 資料 強健性、臨床 證據 compensation 與 決策輔助 explainability。
## 2026-06-21 Task2/Task3 共用資料來源更新

- `task2_3_pure_data(6_24).xlsx` 現在是 Task2 與 Task3 的共用 raw source。
- 目前檔案雖曾有 `.csv` 延伸版本，但內容是 Excel/OpenXML；`preprocessing_v74.load_tabular_data()` 可在不額外要求 `openpyxl` 的情況下讀取。
- `NA`、`na`、`N/A`、`n/a` 現在都是 rule parsing 與 label cleaning 中明確的 缺失 tokens。
- 目前 smoke check：Task2 standard rows = 384、Task2 rule/label mismatch = 27；Task3 valid A/B/C rows = 271，且許多 rows 因 tympanogram 證據 缺失而由 規則暫不判讀。
- 解讀提醒：Task3 應分開看 規則覆蓋 performance 與 缺失-證據 暫不判讀；Task2 應分開看 standard ontology rows 與 high-tone/low-tone loss、MIHL、空白、NA 等排除 labels。

<!-- 2026-06-27-p0-p4-final-alignment -->

## 2026-06-27 P0-P4 收斂後最新說明

### 最新研究定位
本專案目前應定位為：**Missing-aware clinical-rule-guided hybrid decision-support framework for ear-level hearing classification under incomplete audiological evidence**。核心不是單純證明 Transformer 或 MetaIRL 分數較高，而是證明在不完整聽力學資料下，系統能結合臨床規則、模型預測、規則/模型衝突警示、低信心度暫不判讀與 locked-test 可追溯性。

### 目前新增或強化的證據鏈
- `ml_baseline_locked_test_summary_5seed.csv`：補齊 classical ML 在 locked-test 上的公平比較。
- `main_hybrid_summary*.csv`：新增 rule availability、rule failure、rule correction、model fallback success、rule-model conflict、warning rate 等臨床價值指標。
- `hybrid_threshold_sweep*.csv`：用 threshold sweep 說明 confidence gate 不是任意指定。
- `hybrid_strategy_mcnemar*.csv`：提供 strategy-level paired comparison / McNemar 檢定。
- `clinical_error_taxonomy_summary.csv`：將 true label / rule label / model prediction 整理成臨床錯誤 taxonomy。
- `calibration_policy_summary.csv`：把 confidence threshold curve 收斂成可引用的 policy summary。
- `statistical_summary_all_configs.csv`：新增 bootstrap CI 欄位。
- `artificial_missingness_v74.py` 與 `feature_importance_v74.py`：主流程預設改用 `run_13_tiny_m15_bc_dominant_r010/no_meta_seed_*` 作為 primary 5-seed 分析基礎。

### 論文敘事應避免過度主張
- 不應主張 device shift 已完成驗證，除非後續補上 device label 或 cross-device validation。
- 不應主張 Transformer / MetaIRL / IRL 本身是唯一主要貢獻；這些應寫成 neural component 或 ablation factor。
- 不應主張 hybrid 一定全面擊敗 clinical rule；應主張 hybrid 的價值在 rule coverage、model fallback、conflict warning 與 incomplete-data tolerance。
- 邊緣端 latency/profile 目前可作補充，不作本輪主軸；若要寫 IEEE IoT/IoMT，需以概念性 IoMT decision-support pipeline、missingness robustness 與 clinical warning 為主。

### 建議論文主線
1. Subject-level / locked-test traceable split 建立可信評估基礎。
2. Clinical rule baseline 建立可解釋診斷參考。
3. Neural model 補足 rule 無法完整判斷或資料不完整的案例。
4. Hybrid rule-guided framework 整合 rule decision、model fallback、warning 與 abstention。
5. Missingness robustness、feature importance、calibration 與 statistical tests 支撐系統可靠性。

<!-- /2026-06-27-p0-p4-final-alignment -->

<!-- 2026-07-01-task1-missingness-alignment -->
## 2026-07-01 Task1 缺失訓練與分析對齊

缺失感知流程現在明確涵蓋 Task1/Task2/Task3。Task1 在訓練階段使用 `mask_pta`、`mask_high_freq`、`mask_low_freq`、`mask_all_ac` 做 feature masking；人工缺失分析新增對應的 `task1_no_pta`、`task1_no_high_freq`、`task1_no_low_freq`、`task1_no_all_ac`，用來衡量 PTA、高頻 AC、低頻 AC、全部 AC 缺失時的性能下降。

Task1 缺失是數值遮蔽型 robustness analysis；Task2/Task3 則同時包含 missing flag、NR/censored 或 NP-like evidence。論文架構圖可以把三者都放在 incomplete audiological evidence robustness，但文字需區分缺失機制不同。
<!-- /2026-07-01-task1-missingness-alignment -->
